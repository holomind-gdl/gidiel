/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure API routes and dynamic pages work correctly on Vercel
  // Do NOT set output: 'export' — it breaks Server Components and API routes
};

export default nextConfig;
